# Ignore database JSON files
*.json

# But keep important ones (if needed)
!package.json

# Node modules (don't upload dependencies)
node_modules/

# OS & IDE files
.DS_Store
.vscode/
.env

# Expo & React Native
.expo/
.expo-shared/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase config (optional)
firebase.json
firestore.indexes.json
firestore.rules
